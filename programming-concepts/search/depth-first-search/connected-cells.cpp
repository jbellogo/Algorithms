// given a matrix of zeros and ones, determine size of the biggest region of 1s, 
// which may be connected vertically, horizontally, or diagonally. 


/*
Idea: 
    - Lets iterate through x and y in quadratic time, 
      when we have a one, we check its nbrs and recurse on the first one that is one. 
*/

#include<vector>

using namespace std;

class Solution{
    public:
    int getBiggestRegion(vector<vector<int>> &matrix, int numRows, int numCols){
        for (int row = 0; row < numRows; row++){

        }


    }
}